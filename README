这是一个电商平台的后台管理系统，是一个练手项目，如果要下载运行请先下载数据库文件，在您的pc配置好数据之后，下载node服务端文件
打开cmd输入 npm install 安装服务端依赖
nodemon app运行服务端
成功运行起服务端之后，再下载配置客户端(后台管理系统),同样也要安装依赖npm install。
整个项目使用了vue框架，ui是element ui，后端是node，数据库是mysql
